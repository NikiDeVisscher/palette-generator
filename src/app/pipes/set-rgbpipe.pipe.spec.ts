import { SetRGBpipePipe } from './set-rgbpipe.pipe';

describe('SetRGBpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SetRGBpipePipe();
    expect(pipe).toBeTruthy();
  });
});
